[tox]
requires =
    tox>=4.2
env_list =
    py{313, 312, 311, 310}

[testenv]
deps =
    allianceauth
    coverage[toml]
    django-webtest
set_env =
    DJANGO_SETTINGS_MODULE = testauth.settings.local
commands =
    coverage run runtests.py aa_skyhook_monitor -v 2
    coverage report
    coverage xml
install_command = python -m pip install -U {opts} {packages}
